# Dependências
node_modules/
.pnpm-store/

# Build / saída
dist/
build/
*.tsbuildinfo

# Variáveis de ambiente (commitar só os *.example)
.env
.env.*
!.env.example

# Logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*

# Cobertura / testes
coverage/
.nyc_output/

# Documentos pessoais largados na raiz — NUNCA versionar.
# Fatura/recibo da Stripe traz nome, endereço e final do cartão; um `git add -A`
# distraído publicaria isso no histórico, de onde não sai mais. Regras ancoradas
# na raiz (a barra inicial) para não esconder PDF legítimo dentro do projeto.
/Invoice-*.pdf
/Receipt-*.pdf
/EDITAL.pdf

# Editor / SO
.DS_Store
Thumbs.db
.idea/
.vscode/
