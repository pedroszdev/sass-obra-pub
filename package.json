{
  "name": "sass-obra-pub",
  "version": "0.0.0",
  "private": true,
  "description": "SaaS de captação e busca de editais de obra pública para empreiteiros",
  "license": "UNLICENSED",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  },
  "scripts": {
    "lint": "pnpm -r --if-present lint",
    "test": "pnpm -r --if-present test",
    "build": "pnpm -r --if-present build"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["bcrypt"]
  }
}
